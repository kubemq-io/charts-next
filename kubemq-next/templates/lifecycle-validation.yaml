{{- if and .Values.operator.enabled .Values.cluster.enabled }}
{{- fail "operator and cluster must use independent releases: install operator with cluster.enabled=false, then cluster with operator.enabled=false" }}
{{- end }}
{{- if and .Values.preDelete (.Values.preDelete.enabled | default false) }}
{{- fail "preDelete hooks are removed: omit preDelete, retain the cluster and delete its recorded UID explicitly before uninstall" }}
{{- end }}
